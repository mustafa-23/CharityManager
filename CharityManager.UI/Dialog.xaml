<Window x:Class="CharityManager.UI.Dialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CharityManager.UI"
        xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
        mc:Ignorable="d"
        FlowDirection="{DynamicResource AppDirection}"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowState="Maximized"
        StateChanged="Window_StateChanged"
        Loaded="Dialog_Loaded"
        Height="450" Width="800">

    <Window.Background>
        <SolidColorBrush Color="{DynamicResource Colors.Accent}" Opacity="0.4"/>
    </Window.Background>

    <Window.InputBindings>
        <KeyBinding Key="Esc" Command="{x:Static local:Commands.CloseDialog}"/>
    </Window.InputBindings>
    
    <Grid dxmvvm:UIRegion.Region="{x:Static local:AppRegions.Dialog}"/>
</Window>
